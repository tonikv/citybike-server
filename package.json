{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend service for citybike data",
  "main": "server.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server.js ",
    "start": "node server.js",
    "test": "cross-env NODE_ENV=test jest --testTimeout=10000 --detectOpenHandles --watchAll --maxworkers=1"
  },
  "keywords": [
    "express",
    "mongoose"
  ],
  "author": "Toni Virtanen",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongoose": "^6.5.1",
    "papaparse": "^5.3.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.3.1",
    "mockingoose": "^2.16.2",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
